[project]
name = "backend"
version = "0.1.0"
description = "Infinite Chess Matchmaking Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0",
    "channels>=4.0",
    "daphne>=4.1",
    "django-cors-headers>=4.3",
    "channels-redis>=4.2",
]
